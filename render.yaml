services:
  - type: web
    name: newsletter-api-v2
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 -m uvicorn app.main:web_app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        value: "postgresql://newsletter_v2_db_user:CCXs3mr9SjZ0YEdpOolTPEX2SMBuE2IF@dpg-d3fallpgv73c73apqhag-a.virginia-postgres.render.com/newsletter_v2_db"
      - key: OPENAI_API_KEY
        sync: false  # You'll set this manually in Render dashboard
      - key: GOOGLE_CSE_API_KEY
        sync: false  # Optional - set if you want CSE features
      - key: GOOGLE_CSE_CX_ID
        sync: false  # Optional - set if you want CSE features
